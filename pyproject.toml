[project]
name = "tools-used-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hussainezzi", email = "hussainezzi03@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "chainlit>=2.4.400",
    "openai-agents>=0.0.9",
    "python-dotenv>=1.1.0",
]

[project.scripts]
tools-used-agents = "tools_used_agents:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
